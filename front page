<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tic Tac Toe: Apocalypse</title>
    <link href="https://fonts.googleapis.com/css2?family=Nosifer&family=Stardos+Stencil:wght@700&display=swap" rel="stylesheet">
    <style>
        :root {
            --zombie-green: #3dff00;
            --soldier-blue: #00a2ff;
            --blood-red: #ff0000;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            height: 100vh;
            background: #000;
            overflow: hidden;
            font-family: 'Arial', sans-serif;
            color: white;
        }

        /* Title at the Top */
        .game-header {
            position: absolute;
            top: 20px;
            width: 100%;
            text-align: center;
            z-index: 20;
        }

        .game-title {
            font-family: 'Nosifer', cursive;
            font-size: 5rem;
            letter-spacing: 10px;
            background: linear-gradient(to right, var(--zombie-green), #fff, var(--soldier-blue));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            filter: drop-shadow(0 0 10px rgba(255,255,255,0.5));
        }

        /* Split Screen Container */
        .war-zone {
            display: flex;
            width: 100%;
            height: 100%;
        }

        .army-side {
            flex: 1;
            position: relative;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden;
        }

        /* Zombie Side (Left) */
        .zombies {
            background: linear-gradient(to right, rgba(0,20,0,0.9), transparent), 
                        url('https://www.transparenttextures.com/patterns/asfalt-dark.png');
            border-right: 2px solid var(--zombie-green);
        }

        /* Soldier Side (Right) */
        .soldiers {
            background: linear-gradient(to left, rgba(0,20,40,0.9), transparent),
                        url('https://www.transparenttextures.com/patterns/asfalt-dark.png');
            border-left: 2px solid var(--soldier-blue);
        }

        /* Animated Army Elements */
        .character {
            position: absolute;
            width: 120px;
            filter: drop-shadow(0 0 15px black);
            animation: breathe 3s ease-in-out infinite alternate;
        }

        /* Individual placements to simulate an "Army" */
        .z1 { left: 10%; top: 30%; font-size: 80px; }
        .z2 { left: 25%; top: 50%; font-size: 90px; animation-delay: 0.5s; }
        .z3 { left: 5%; top: 70%; font-size: 70px; animation-delay: 1.2s; }

        .s1 { right: 10%; top: 30%; font-size: 80px; }
        .s2 { right: 25%; top: 50%; font-size: 90px; animation-delay: 0.7s; }
        .s3 { right: 5%; top: 70%; font-size: 70px; animation-delay: 1.5s; }

        @keyframes breathe {
            from { transform: translateY(0) scale(1); filter: brightness(1); }
            to { transform: translateY(-15px) scale(1.05); filter: brightness(1.3); }
        }

        /* Center Button */
        .ui-overlay {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            z-index: 30;
            text-align: center;
        }

        .start-btn {
            background: var(--blood-red);
            color: white;
            font-family: 'Stardos Stencil', cursive;
            font-size: 3rem;
            padding: 25px 80px;
            border: 5px solid white;
            border-radius: 50px;
            cursor: pointer;
            box-shadow: 0 0 40px var(--blood-red);
            transition: 0.3s;
            text-transform: uppercase;
        }

        .start-btn:hover {
            transform: scale(1.1) rotate(-2deg);
            background: white;
            color: var(--blood-red);
            box-shadow: 0 0 60px white;
        }

        .vs-circle {
            background: #000;
            border: 3px solid #fff;
            width: 80px;
            height: 80px;
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: 'Nosifer', cursive;
            font-size: 2rem;
            margin: 0 auto 20px;
        }
    </style>
</head>
<body>

    <header class="game-header">
        <h1 class="game-title">TIC TAC TOE</h1>
    </header>

    <div class="war-zone">
        <section class="army-side zombies">
            <div class="character z1">🧟</div>
            <div class="character z2">🧟‍♂️</div>
            <div class="character z3">🧟‍♀️</div>
            <div class="character" style="left:35%; top:20%; font-size:60px;">🧟</div>
        </section>

        <section class="army-side soldiers">
            <div class="character s1">🪖</div>
            <div class="character s2">🔫</div>
            <div class="character s3">🎖️</div>
            <div class="character" style="right:35%; top:75%; font-size:60px;">🛡️</div>
        </section>
    </div>

    <div class="ui-overlay">
        <div class="vs-circle">VS</div>
        <button class="start-btn" onclick="startGame()">Start Game</button>
    </div>

    <script>
        function startGame() {
            // Visual feedback before transition
            document.querySelector('.start-btn').innerHTML = "DEPLOYING...";
            document.querySelector('.zombies').style.transform = "translateX(-100%)";
            document.querySelector('.soldiers').style.transform = "translateX(100%)";
            
            setTimeout(() => {
                alert("The Battle Begins!");
                // You would link your tic-tac-toe game logic here
            }, 600);
        }
    </script>

</body>
</html>
